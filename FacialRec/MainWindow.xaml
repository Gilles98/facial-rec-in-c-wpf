<Window x:Class="FacialRec.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FacialRec"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="2.5*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical" Grid.Column="1" Margin="20 10 10 20">
            <Label Content="Camera:"/>
            <ComboBox Grid.Column="1" Margin="5" ItemsSource="{Binding LijstApparaten}" SelectedIndex="{Binding SelectedCamera}">
            </ComboBox>
        </StackPanel>
        <Image Source ="{Binding Bi}" Grid.Row="1" Grid.Column="1" Margin="5"/>
        <Button CommandParameter="Camera" Command="{Binding}" Margin="20">start</Button>
    </Grid>
</Window>
